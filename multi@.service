[Unit]
Description=Starts two docker containers for our Sandbox
After=docker.service

[Service]
TimeoutStartSec=0
ExecStartPre=-/usr/bin/docker kill velocity:1.0
ExecStart=/usr/bin/docker run velocity:1.0
ExecStop=/usr/bin/docker stop velocity:1.0